arr = list(map(int, input().split()))

small = large = arr[0]

for x in arr[1:]:
    if x < small:
        small = x

    if x > large:
        large = x

print("Minimum:", small)
print("Maximum:", large)
