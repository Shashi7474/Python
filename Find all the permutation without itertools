def permutations(s):
    if len(s) == 1:
        return [s]

    result = []

    for i in range(len(s)):
        remaining = s[:i] + s[i+1:]

        for p in permutations(remaining):
            result.append(s[i] + p)

    return result


s = input()

for p in permutations(s):
    print(p)
