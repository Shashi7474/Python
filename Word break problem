s = input()
words = set(input().split())

dp = [False] * (len(s) + 1)
dp[0] = True

for i in range(1, len(s) + 1):
    for j in range(i):
        if dp[j] and s[j:i] in words:
            dp[i] = True
            break

print("Possible" if dp[-1] else "Not Possible")
