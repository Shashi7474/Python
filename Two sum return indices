arr = list(map(int, input().split()))
target = int(input())

seen = {}

for i, num in enumerate(arr):
    needed = target - num

    if needed in seen:
        print(seen[needed], i)
        break

    seen[num] = i
