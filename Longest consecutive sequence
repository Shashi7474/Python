arr = list(map(int, input().split()))
nums = set(arr)

longest = 0

for x in nums:
    if x - 1 not in nums:
        current = x

        while current in nums:
            current += 1

        longest = max(longest, current - x)

print(longest)
